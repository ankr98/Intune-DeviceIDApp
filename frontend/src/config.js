// src/config.js

// We use a relative path. The browser will automatically append this 
// to whatever domain the user is currently on (localhost, 10.10.10.10, or intune.acme.com).
export const API_URL = "/api";